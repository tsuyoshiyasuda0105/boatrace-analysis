# P1-3 DB shim hardening work log (2026-08-14)

## Scope and safeguards

- Canonical repository: `C:\boat_project\boatrace-analysis`
- Branch: local `main`; pushing to `origin/main` is prohibited for this task.
- Allowed edit scope: `src/db/connection.py`, new tests under `tests/`, and this log.
- Planned code files: `src/db/connection.py`, `tests/test_db_shim_rewriter.py`, `tests/test_db_pk_map_parity.py`.
- Out of scope: callers, schema migrations, `src/web/app.py`, collectors, scripts, ROI logic, and prediction logic.
- Starting state: `main` matched `origin/main`; unrelated untracked report files are preserved.

## Pre-change findings

Primary keys were verified from DDL and the production-shaped SQLite database in read-only immutable mode:

| Table | Verified primary key | Evidence |
| --- | --- | --- |
| `l4_daily_summary` | `date` | `src/db/schema.sql` and SQLite |
| `course1_stats_cache` | `racer_number, as_of_date` | `scripts/cache_course1_stats.py` and SQLite |
| `decay_factor` | `bucket` | `src/analysis/decay_factor.py`; table absent from current SQLite |
| `paper_trades` | `id` | `scripts/odds_scheduler.py` and SQLite |
| `alert_sent` | `email_hash, race_id, alert_type` | `src/db/schema.sql` and SQLite |
| `racer_entry_change_snapshots` | `snapshot_date, racer_number` | `src/db/schema.sql` and SQLite |

`racer_accident_period_stats` is inconsistent: the live SQLite primary key is
`racer_number, period_year, period_half, rule_version, source_kind`, while the shim map and
`src/db/schema.sql` include `period_end`. Per the task instruction, the shim map will be aligned
to the live SQLite primary key without changing `schema.sql`.

## Test results

- Baseline: `.venv/Scripts/python.exe -m pytest tests/ -q`
  - `16 failed, 576 passed, 1 skipped, 42 errors`
  - The 42 setup errors are pre-existing Windows permission failures under
    `C:\Users\tsuyo\AppData\Local\Temp\pytest-of-tsuyo`.
- Focused new tests: `9 passed`.
- DB connection plus new tests: `14 passed`.
- Final full suite: `.venv/Scripts/python.exe -m pytest tests/ -q`
  - `16 failed, 585 passed, 1 skipped, 42 errors`
  - The same 16 failure names and 42 environment errors remained; no regression was added.
- `git diff --check`: passed.
- Direct `py_compile` could not write the existing protected `src/db/__pycache__` directory.
  Pytest successfully imported and executed the changed module, so syntax and imports were
  verified without altering that unrelated directory.

## Changes and deferred items

- Added verified primary-key mappings for all six reported tables.
- Changed unknown `INSERT OR REPLACE` targets from silent `DO NOTHING` to `ValueError`;
  unknown `INSERT OR IGNORE` targets retain `DO NOTHING` semantics.
- Removed only terminal `--` comments before appending `ON CONFLICT`; embedded line comments
  and quoted comment markers are preserved.
- Aligned `racer_accident_period_stats` to the live SQLite primary key by removing
  `period_end` from the shim mapping.
- Escaped literal percent signs only when psycopg parameter binding is used, preserving `%s`
  markers and already escaped `%%` sequences.
- Added a repository source scan that fails when a static `INSERT OR REPLACE/IGNORE` target
  is absent from `_TABLE_PRIMARY_KEYS`.
- Deferred: `src/db/schema.sql` still includes `period_end` in the
  `racer_accident_period_stats` primary key. Schema changes are prohibited in this task;
  production Postgres constraint parity should be checked in a separate, explicitly approved
  migration task before changing either schema.
- No caller changes were required. No out-of-scope files were edited.

## Delivery status

Implementation and verification are complete. Deployment is waiting. No push was performed.
