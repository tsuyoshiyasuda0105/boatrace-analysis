# P0-3 BAN 止血 — 実装状況と残作業プラン

## 状況

作業指示書 `reports/p0_3_ban_mitigation_plan_20260814.md` の全 6 タスクは**実装・テスト完了済み**
(プランモード発動前に完了)。焦点テスト 43 件すべて green:

```
tests/test_result_html_kimarite.py (15) / test_shared_rate_limit.py (11) /
test_beforeinfo_parts_guard.py (4) / test_odds_cron_lock.py (3) /
test_result_scraper_fail_closed.py (2) / test_result_poll_timezone.py (2) /
test_result_scraper_market_signal_targets.py (6)
→ 43 passed in 0.63s
```

`tests/test_source_regression.py` の result_scraper 静的チェック
(class_number=1 / stadium_number NOT IN / prob_first BETWEEN) は全て保持済みで衝突なし。

## 変更済みファイル (作業ツリーに未コミットで存在)

1. **src/parsers/result_html.py** — `parse_kimarite()` 実装 (テーブル抽出 + テキスト
   フォールバック、KIMARITE_VALUES 6種の完全一致のみ採用、例外安全)。
   `"race_kimarite": None` ハードコードを抽出値に置換。
2. **src/collectors/result_scraper.py** —
   - タスク1安全弁: kimarite バックフィルの 1 レースあたり再試行上限
     `KIMARITE_MAX_ATTEMPTS = 5` (page_html_cache の `kimarite_retry:<race_id>` キーで
     プロセス横断カウント、best-effort・失敗時は従来動作)。
   - タスク3: 候補クエリ失敗時の fail-open (`l4_only = False`) を廃止し fail-closed 化
     (`candidate_lookup_failed` フラグで候補以外を当該パスでは一切取得しない)。
3. **src/collectors/_http.py** — タスク2: プロセス横断共有レートリミッタ。
   Postgres 時 (env `BOATRACE_SHARED_RATE_LIMIT` デフォルト有効) は
   `scrape_rate_slots(host PK, last_request_at)` 1行/host を
   `UPDATE ... WHERE last_request_at <= now() - make_interval(secs => ?) RETURNING 1`
   の CAS で奪い合い (0.4s poll / 30s 上限)。host 単位 (boatrace.jp / mbrace.or.jp)。
   DB 不通時は 300s クールダウン付きでプロセス内リミッタへ自動フォールバック。
   接続は `connect(direct=True)` を使い回し。REQUEST_INTERVAL_SECONDS = 2.0 不変。
4. **src/collectors/official_dl.py** — タスク4: `download_lzh` を `_wait_interval(host)`
   経由に変更 (fetch_one/直呼びでも間隔制御)。`fetch_range` の独自 1.5s sleep 廃止
   (共有リミッタに一本化)。User-Agent は従来から config.USER_AGENT 使用。
5. **scripts/odds_scheduler_render.py** — タスク5: `pg_try_advisory_lock` ガード
   (render_maintenance_scheduler と同型、LOCK_NAME="boatrace-odds-scheduler-v1")。
   取得失敗時は base.main() を呼ばずスキップ・成功記録なし・exit 0。
6. **src/collectors/beforeinfo.py** — タスク6: `_upsert_parts` はパース結果が空なら
   DELETE せずスキップ (既存行があれば warning)。

新規テスト: tests/test_result_html_kimarite.py, tests/test_shared_rate_limit.py,
tests/test_beforeinfo_parts_guard.py, tests/test_odds_cron_lock.py,
tests/test_result_scraper_fail_closed.py

## 残作業 (実行モード復帰後にやること — これだけ)

1. `reports/p0_3_work_log_20260814.md` を書く。内容:
   - 上記の変更ファイル一覧とタスク対応
   - テスト結果 (43 passed / 焦点テストのみ、フル suite は親セッションが実行)
   - デビエーション:
     (a) 決まり手の実ページ HTML がローカルに未保存 (data/raw・page_html_cache とも
         JSON 要約のみ) のため、パーサー既知の DOM 構造から最小 fixture を構築。
         実ページ保存後に fixture 差し替え推奨。
     (b) fail-closed は「失敗パスでは非候補を一切取得しない」最強解釈で実装。
     (c) `scrape_rate_slots` テーブルの CREATE TABLE IF NOT EXISTS は指示書タスク2で
         明示指定されたもの (既存スキーマ変更ではない)。
     (d) beforeinfo の warning は「既存行を温存した場合のみ」出す (毎艇の正常な
         空パーツで warning がスパムになるのを回避)。
   - 「デプロイ待ち」の明記、コミット・push はしていないことの明記
2. 最終報告を親に返す (コミットは親セッションが実施)。

これ以外のコード変更は不要。
