# コア依存関係 (本番ランタイムに必要)
requests>=2.31
httpx>=0.28,<1
pandas>=2.0
numpy>=1.24
stripe>=11.0

# 機械学習
lightgbm>=4.0
# Pickled calibration models were saved with 1.8.0; keep Render aligned.
scikit-learn==1.8.0

# スクレイピング (Layer 3)
beautifulsoup4>=4.12
lxml>=5.0

# Web UI (予測表示)
flask>=3.0
gunicorn>=21.2     # 本番用 WSGI サーバー (Render / Heroku 等)

# DB (SQLite ローカル開発 / PostgreSQL 本番)
# DATABASE_URL=postgresql://... が指定された場合のみ psycopg を使用
psycopg[binary]>=3.1     # psycopg3 (新世代、推奨)
psycopg_pool>=3.2

# メール購読機能 (AES-GCM 暗号化)
cryptography>=42.0

# .env 自動読み込み (ローカル開発時、本番では env var 直設定)
python-dotenv>=1.0

# LZH 解凍 (Layer 1: 公式DLファイル用)
# Render/Linux では Python で展開し、7-Zip はローカルの予備経路に限定する
lhafile>=0.3.1
