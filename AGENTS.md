# Repository Work Rules

- Search with `rg` before reading broad areas.
- Keep changes scoped to the requested behavior.
- Do not use a local PC as a production scheduler or Supabase writer.
- Verify cron changes with targeted tests and production data integrity checks.
- Record active work, failures, and running tools in `docs/handoff.md`.
